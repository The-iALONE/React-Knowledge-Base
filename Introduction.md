# Introduction

آشنایی با React و جایگاه آن در اکوسیستم فرانت‌اند.

---

## 📖 مفهوم

کتابخانه JavaScript برای ساخت رابط کاربری (UI) است؛ در اکوسیستم React کار می‌کند. بر پایه کامپوننت، `declarative` و `component-based` است.

---

## چرا این ویژگی وجود دارد؟

قبل از React، DOM به‌صورت `imperative` دستکاری می‌شد. با `declarative programming` و Virtual DOM، React ساخت UI پیچیده را ساده‌تر کرد.

---

## چه مشکلی را حل می‌کند؟

مدیریت `state` و DOM در اپلیکیشن‌های بزرگ؛ همگام‌سازی UI با داده.

---

## ⚙️ نحوه کار

شما `state` را تعریف می‌کنید → React UI را بر اساس `state` `render` می‌کند → با تغییر `state`، React فقط بخش‌های لازم را به‌روز می‌کند.

---

## چه زمانی استفاده کنیم؟

ساخت SPA، dashboard، فرم‌های پیچیده، UI تعاملی.

---

## چه زمانی استفاده نکنیم؟

صفحات استاتیک ساده بدون تعامل (HTML/CSS کافی است).

---

## ارتباط با مفاهیم دیگر

- [Quick Start](./Quick-Start.md)
- [Components](./Components.md)
- [Thinking in React](./Thinking-in-React.md)

---

## خلاصه

کتابخانه UI مبتنی بر کامپوننت در React = `declarative` + Virtual DOM.

---

## 📚 منابع

- [React Documentation](https://react.dev)
